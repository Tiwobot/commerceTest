import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Products - Yangfinity",
  description: "Buy Metin2 Yang & Won for all servers. Gameforge West, East, TR, Private servers, and Mobile Metin2. Fast delivery, secure payment."
};

export default function ProductsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
